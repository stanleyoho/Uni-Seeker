/**
 * STUB — auto-generated OpenAPI TypeScript types.
 *
 * This file is a placeholder until `openapi-typescript` is installed and the
 * generation script is run. Do NOT hand-edit; it will be overwritten.
 *
 * To regenerate:
 *   1. Ensure backend is running:    cd backend && uv run uvicorn app.main:app --port 8000
 *   2. Install dependency once:      cd frontend && npm install
 *   3. Run generation:               npm run gen:api-types
 *
 * After generation, this file will export `paths`, `components`, `operations`,
 * and `webhooks` namespaces. See ./README.md for usage patterns.
 */

// Empty placeholder exports so downstream `import type { paths, components }`
// statements compile cleanly before the first real generation.
export type paths = Record<string, never>;
export type components = {
  schemas: Record<string, never>;
};
export type operations = Record<string, never>;
export type webhooks = Record<string, never>;
